export class List {
    /*
    Should be able to
    -create with length
    -return length
    -access element (input and return contents)
    -add element to end
    -concatenate two lists
    */

    /*Structure: a head with a chain of elements I guess*/

    constructor(length: int<?) {
        this.length = length;
    }

    add(a:number, b:number):number {
        return a+b
    }
}